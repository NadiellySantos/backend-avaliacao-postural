name: Deploy para Azure App Service

on:
  push:
    branches:
      - main  # ✅ troque se seu branch principal for outro (ex: master)

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      # ✅ LOGIN NO AZURE — usa o segredo que você vai criar no GitHub (AZURE_CREDENTIALS)
      - name: Login no Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # ✅ Python — escolha a versão usada no seu projeto (pode deixar 3.12 normalmente)
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # ✅ DEPLOY PARA O APP SERVICE
      - name: Fazer deploy no Azure
        uses: azure/webapps-deploy@v3
        with:
          app-name: backend-avaliacao-postural         # 🔸 troque para o nome exato do App Service (ex: meuappflask)
          slot-name: Production             # 🔸 normalmente é "Production", pode deixar assim


